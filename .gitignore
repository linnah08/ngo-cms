site.config.php
smtp.config.php
graph.config.php
db.config.php
courier.config.php
local.config.php
deploy.php
*.log
php_errors.log
.DS_Store
/assets/images/uploads/
admin/setup.php
git-reset.php
run-migrate.php
schema.sql
migrate-from-woo.php

/vendor/
.phpunit.result.cache
.cpanel.yml

.idea/

# Playwright
.playwright-auth.json
/test-results/
/playwright-report/

# Node
node_modules/
package-lock.json

# Generated documents (served via admin/download-document.php, not committed)
/documents/invoices/*/
/documents/receipts/*/
/documents/donation_certs/*/
/documents/tickets/*/
/documents/boxnow_batches/

# Admin-editable content files — server is source of truth, git reset must not overwrite these
/content/pages.json
/content/home.json
/content/home.json.lock
/content/home.json.tmp-*
/content/home.json.corrupt-*
/assets/images/pages/home/
/content/menus.json
/content/organisation.json

# MCP server configuration with credentials
.mcp.json

# Operator scripts (prod tunnel, prod sync, worktree helper) and their artifacts.
# Removed from the published repo in 7f3205e "Scrub personal infra" because they
# name this operator's own server and SSH alias. They are still needed locally —
# CLAUDE.md's "Database querying" section documents them — so they live here
# untracked rather than being deleted again.
scripts/

# Self-update mechanism (includes/updater.php) — runtime state, not source.
# checksums.json ships inside the release ZIP (generated by build-release.yml)
# and lands in the project root when a release is unzipped or applied.
/checksums.json
/backups/
/.maintenance
/logs/update-check-cache.json
