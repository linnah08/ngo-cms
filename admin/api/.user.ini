; auto_prepend_file resolves relative to the executing script's own directory,
; not the directory the .user.ini lives in — so every admin/<subdir>/ needs
; its own copy of this pair (see admin/.user.ini + admin/pin-docroot.php).
auto_prepend_file = pin-docroot.php
